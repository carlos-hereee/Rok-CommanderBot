// const
// export = {
//   name: "8 Ball",
//   triggers: [`ball`, "8ball"],
//   description: "Get mystical predictions for a yes or no question.",
//   handler: (message: { reply: (arg0: string) => any; author: any; }) => {
//     const answer = prediction[Math.floor(Math.random() * prediction.length)];
//     return message.reply(`${message.author} ${answer} 🎱`);
//   },
// };
