// import { SlashCommandBuilder } from "discord.js";

// export default {
//     name: "Play ping pong",
//     description: "Play ping pong",
//     triggers: ["ping", "pong"],
//     handler: async (message: { reply: (arg0: string) => any; }, trigger: string) => {
//         const reply = trigger ==="ping"? "pong":"ping"
//    return message.reply(reply);
//     },
//   };
  